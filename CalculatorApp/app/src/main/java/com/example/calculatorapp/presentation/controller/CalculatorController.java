package com.example.calculatorapp.presentation.controller;

import com.example.calculatorapp.domain.exception.NumException;
import com.example.calculatorapp.domain.model.CalculatorModel;
import com.example.calculatorapp.presentation.validator.Validator;

public class CalculatorController {
    private Validator<CalculatorModel> numValidator;

    public CalculatorController() {

    }

    public void validateInput(CalculatorModel calculatorModel) {

    }
}
