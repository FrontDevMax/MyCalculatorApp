package com.example.calculatorapp.domain.enumeration;

public enum AuthError {
    INVALID_EMAIL,
    INVALID_PASSWORD,
    INVALID_CONFIRM_PASSWORD,
    INVALID_USERNAME,
    LOGIN_FAILED,
    REGISTRATION_FAILED
}
